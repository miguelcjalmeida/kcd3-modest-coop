# .NET build output (agent/ has its own copy of these rules too)
bin/
obj/
*.user

# Agent's runtime config (contains a shared secret, and is per-machine/per-session)
itemswap-agent.json

# OS/editor cruft
.vs/
.vscode/
*.DotSettings.user
Thumbs.db
